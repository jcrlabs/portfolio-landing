apiVersion: v1
kind: Service
metadata:
  name: {{ include "portfolio-landing.fullname" . }}
  labels:
    {{- include "portfolio-landing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
  selector:
    {{- include "portfolio-landing.selectorLabels" . | nindent 4 }}
