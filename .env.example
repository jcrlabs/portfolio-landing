# Sin secrets — los datos se obtienen en build time via ISR
# Para desarrollo local:
# No se necesita nada adicional

# Demo iframe tokens — API base URLs (sin trailing slash)
NEXT_PUBLIC_INVENTORY_API_URL=https://api.electroteca.jcrlabs.net
NEXT_PUBLIC_CHAT_API_URL=https://api.chat.jcrlabs.net
